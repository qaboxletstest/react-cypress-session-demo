import React from 'react';
import "./SessionDemo.css"

export default function SessionDemo() {
    return (
        <div className="wrapper">
            <h2>session</h2>
            <p>Cache and restore cookies, localStorage, and sessionStorage in order to reduce test setup times.</p>
        </div>
    );
}